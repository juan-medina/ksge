plugins {
    id 'org.jetbrains.kotlin.multiplatform'
}

version '0.0.1'

kotlin {
    linuxX64("linux") {
        binaries {
            executable {
            }
        }
    }
    sourceSets {
        linuxMain {
        }
        shared {
            dependencies {
                implementation kotlin('stdlib-common')
                api project(':ksge')
            }
            linuxMain.dependsOn(it)
        }
    }
}

def taskGroupRunSample = 'examples'

tasks.register("runBasicLinuxDebug") {
    group = taskGroupRunSample
    dependsOn("runDebugExecutableLinux")
}

tasks.register("runBasicLinuxRelease") {
    group = taskGroupRunSample
    dependsOn("runReleaseExecutableLinux")
}
